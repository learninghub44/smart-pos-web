// Demo seed data removed. All data comes from Supabase (synced to IndexedDB for offline use).
// Use the Inventory, Categories, Brands, Suppliers, and Customers pages to add real data.
export async function seedSampleData() {
  console.warn('seedSampleData() has been removed. Add products via the Inventory page.')
  return 0
}
